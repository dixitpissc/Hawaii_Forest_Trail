# config/mapping/class_mapping.py

CLASS_COLUMN_MAPPING = {
    "Name": "Name",
    "Active": "Active",
    "FullyQualifiedName": "FullyQualifiedName"
    # QBO only accepts limited fields for Class — Parent is sent separately as ParentRef
}
